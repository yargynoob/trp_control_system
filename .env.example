# TRP Control System - Environment Configuration
# Скопируйте этот файл в .env и заполните значения

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=TRP
POSTGRES_HOST=localhost
POSTGRES_PORT=5169

# ===========================================
# Security (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
# ===========================================
# Генерация секретного ключа:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE-THIS-TO-RANDOM-SECRET-KEY-MIN-32-CHARS

# Время жизни токена в минутах (10080 = 7 дней)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ===========================================
# Application Settings
# ===========================================
DEBUG=False
ENVIRONMENT=production

# ===========================================
# CORS Settings
# ===========================================
# Для production укажите домен вашего сайта
BACKEND_CORS_ORIGINS=["http://localhost","http://localhost:3000","https://yourdomain.com"]

# ===========================================
# Frontend API URL
# ===========================================
# Для Docker: http://localhost/api
# Для локальной разработки: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost/api

# ===========================================
# Email Configuration (Optional)
# ===========================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ===========================================
# Ports (для Docker)
# ===========================================
HTTP_PORT=80
HTTPS_PORT=443

# ===========================================
# PostgreSQL Tools Path (Optional - для резервного копирования)
# ===========================================
# Укажите путь к директории bin PostgreSQL, если pg_dump не в PATH
# Windows: C:\Program Files\PostgreSQL\17\bin
# Linux/Mac: обычно не требуется
PG_BIN_PATH=
